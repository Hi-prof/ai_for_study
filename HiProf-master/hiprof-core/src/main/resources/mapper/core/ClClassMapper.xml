<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hiprof.core.mapper.ClClassMapper">

    <resultMap type="ClClass" id="ClClassResult">
        <result property="classId" column="class_id" />
        <result property="majorId" column="major_id" />
        <result property="majorName" column="major_name" />
        <result property="className" column="class_name" />
        <result property="status" column="status" />
        <result property="remark" column="remark" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectClClassVo">
        select c.class_id,
               c.major_id,
               d.dept_name as major_name,
               c.class_name,
               c.status,
               c.remark,
               c.create_time,
               c.update_time
        from cl_classes c
        left join sys_dept d on d.dept_id = c.major_id
    </sql>

    <select id="selectClClassById" parameterType="Long" resultMap="ClClassResult">
        <include refid="selectClClassVo" />
        where c.class_id = #{classId}
    </select>

    <select id="selectClClassList" parameterType="ClClass" resultMap="ClClassResult">
        <include refid="selectClClassVo" />
        <where>
            <if test="majorId != null">and c.major_id = #{majorId}</if>
            <if test="className != null and className != ''">and c.class_name like concat('%', #{className}, '%')</if>
            <if test="status != null and status != ''">and c.status = #{status}</if>
        </where>
        order by c.major_id asc, c.class_name asc, c.class_id asc
    </select>

    <select id="selectClClassByMajorIdAndClassName" parameterType="ClClass" resultMap="ClClassResult">
        <include refid="selectClClassVo" />
        where c.major_id = #{majorId}
          and c.class_name = #{className}
        limit 1
    </select>

    <insert id="insertClClass" parameterType="ClClass" useGeneratedKeys="true" keyProperty="classId">
        insert into cl_classes
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="majorId != null">major_id,</if>
            <if test="className != null and className != ''">class_name,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null">remark,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="majorId != null">#{majorId},</if>
            <if test="className != null and className != ''">#{className},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateClClass" parameterType="ClClass">
        update cl_classes
        <trim prefix="SET" suffixOverrides=",">
            <if test="majorId != null">major_id = #{majorId},</if>
            <if test="className != null and className != ''">class_name = #{className},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where class_id = #{classId}
    </update>

    <delete id="deleteClClassByIds" parameterType="Long">
        delete from cl_classes where class_id in
        <foreach collection="array" item="classId" open="(" separator="," close=")">
            #{classId}
        </foreach>
    </delete>
</mapper>
