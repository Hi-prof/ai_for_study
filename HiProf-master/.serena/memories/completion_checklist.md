完成任务前至少做这些验证：
1. 后端改动后运行 `mvn -pl hiprof-admin -am -DskipTests compile`
2. 前端页面或接口改动后在 `hiprof-ui` 目录运行 `npm run build:prod`
3. 如果涉及数据库结构，补充 SQL 迁移脚本，并在答复中点明是否需要执行
4. 如果需求涉及权限，除代码/SQL 检查外，尽量核对真实数据库；若数据库不可达，要明确说明无法实锤验证
5. 汇报时区分“已验证通过”和“基于代码推断”两类结论，别瞎吹牛